# Deprecation Notice: Entegrasyon.WinForm

This project (`Entegrasyon.WinForm`) is deprecated and scheduled for removal.

## Migration Plan

1. **Archive Logic**: Ensure no critical business logic is only present in WinForm.
2. **Remove Project**: Delete the WinForm project from the solution.
3. **Update Solution**: Remove references from `Entegrasyon.sln`.

## Current Usage
- Windows Forms UI (legacy).

## Contact
If you need to reintroduce WinForm UI, create an issue with the requirements.
