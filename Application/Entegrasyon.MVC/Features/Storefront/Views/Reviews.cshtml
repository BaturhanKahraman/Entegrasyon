@model List<Entegrasyon.Entity.Storefront.StorefrontReview>


        @* ── Ozet Kartlari ──────────────────────────────────��───── *@
        <div class="row row-deck row-cards mb-3">
            <div class="col-sm-6 col-lg-3">
                <div class="card card-sm">
                    <div class="card-body">
                        <div class="row align-items-center">
                            <div class="col-auto">
                                <span class="bg-primary text-white avatar">
                                    <i class="ti ti-star"></i>
                                </span>
                            </div>
                            <div class="col">
                                <div class="font-weight-medium">@Model.Count Toplam</div>
                                <div class="text-secondary">değerlendirme</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-sm-6 col-lg-3">
                <div class="card card-sm">
                    <div class="card-body">
                        <div class="row align-items-center">
                            <div class="col-auto">
                                <span class="bg-warning text-white avatar">
                                    <i class="ti ti-info-circle"></i>
                                </span>
                            </div>
                            <div class="col">
                                <div class="font-weight-medium">@Model.Count(r => !r.IsApproved) Bekleyen</div>
                                <div class="text-secondary">onay bekliyor</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-sm-6 col-lg-3">
                <div class="card card-sm">
                    <div class="card-body">
                        <div class="row align-items-center">
                            <div class="col-auto">
                                <span class="bg-success text-white avatar">
                                    <i class="ti ti-check"></i>
                                </span>
                            </div>
                            <div class="col">
                                <div class="font-weight-medium">@Model.Count(r => r.IsApproved) Onaylanmış</div>
                                <div class="text-secondary">yayında</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-sm-6 col-lg-3">
                <div class="card card-sm">
                    <div class="card-body">
                        <div class="row align-items-center">
                            <div class="col-auto">
                                <span class="bg-info text-white avatar">
                                    <i class="ti ti-star"></i>
                                </span>
                            </div>
                            <div class="col">
                                @{ var avg = Model.Count > 0 ? Model.Average(r => r.Rating) : 0; }
                                <div class="font-weight-medium">@avg.ToString("F1") Ortalama</div>
                                <div class="text-secondary">puan</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @* ── Değerlendirme Listesi ──────────────────────────────── *@
        <div class="card" hx-get="/storefront/reviews" hx-trigger="reviewsUpdated from:body" hx-select=".card .table-responsive" hx-target=".table-responsive" hx-swap="outerHTML">
            <div class="card-header">
                <h3 class="card-title">Değerlendirmeler</h3>
            </div>
            @if (Model.Count == 0)
            {
                <div class="card-body text-center py-5">
                    <p class="text-secondary">Henüz değerlendirme yok.</p>
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-vcenter card-table table-hover">
                        <thead>
                            <tr>
                                <th>Müşteri</th>
                                <th>Puan</th>
                                <th>Yorum</th>
                                <th>Durum</th>
                                <th>Tarih</th>
                                <th class="w-1">İşlem</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var review in Model.OrderByDescending(r => r.CreatedAt))
                            {
                                <tr>
                                    <td>
                                        <div>
                                            <span class="text-secondary small">ID: @review.CustomerId</span>
                                            @if (review.IsVerifiedPurchase)
                                            {
                                                <span class="badge bg-green-lt ms-1">Doğrulanmış</span>
                                            }
                                        </div>
                                    </td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            @for (var i = 1; i <= 5; i++)
                                            {
                                                if (i <= review.Rating)
                                                {
                                                    <i class="ti ti-star-filled text-yellow"></i>
                                                }
                                                else
                                                {
                                                    <i class="ti ti-star text-muted"></i>
                                                }
                                            }
                                        </div>
                                    </td>
                                    <td style="max-width: 300px;">
                                        @if (!string.IsNullOrEmpty(review.Title))
                                        {
                                            <div class="font-weight-medium">@review.Title</div>
                                        }
                                        <div class="text-secondary text-truncate">@review.Comment</div>
                                        @if (!string.IsNullOrEmpty(review.ReplyText))
                                        {
                                            <div class="mt-1 ps-2 border-start border-primary">
                                                <small class="text-primary">Yanıt: @review.ReplyText</small>
                                            </div>
                                        }
                                    </td>
                                    <td>
                                        @if (review.IsApproved)
                                        {
                                            <span class="badge bg-green-lt">Onaylanmış</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-yellow-lt">Beklemede</span>
                                        }
                                    </td>
                                    <td>
                                        <span class="text-secondary">@review.CreatedAt.ToString("dd.MM.yyyy HH:mm")</span>
                                    </td>
                                    <td>
                                        <div class="btn-list flex-nowrap">
                                            @if (!review.IsApproved)
                                            {
                                                <button class="btn btn-ghost-success btn-sm"
                                                        hx-post="/storefront/reviews/@review.Id/approve"
                                                        hx-swap="none"
                                                        title="Onayla">
                                                    <i class="ti ti-check"></i>
                                                </button>
                                                <button class="btn btn-ghost-danger btn-sm"
                                                        hx-post="/storefront/reviews/@review.Id/reject"
                                                        hx-swap="none"
                                                        hx-confirm="Bu değerlendirmeyi reddetmek istediğinize emin misiniz?"
                                                        title="Reddet">
                                                    <i class="ti ti-x"></i>
                                                </button>
                                            }
                                            @if (string.IsNullOrEmpty(review.ReplyText))
                                            {
                                                <button class="btn btn-ghost-primary btn-sm"
                                                        onclick="showReplyForm(@review.Id)"
                                                        title="Yanıtla">
                                                    <i class="ti ti-message-reply"></i>
                                                </button>
                                            }
                                        </div>
                                    </td>
                                </tr>
                                @if (string.IsNullOrEmpty(review.ReplyText))
                                {
                                    <tr id="reply-row-@review.Id" style="display: none;">
                                        <td colspan="6">
                                            <form class="d-flex gap-2"
                                                  hx-post="/storefront/reviews/@review.Id/reply"
                                                  hx-swap="none">
                                                <input type="text" name="replyText" class="form-control"
                                                       placeholder="Yanıtınızı yazın..." required />
                                                <button type="submit" class="btn btn-primary btn-sm">Gönder</button>
                                                <button type="button" class="btn btn-ghost-secondary btn-sm"
                                                        onclick="document.getElementById('reply-row-@review.Id').style.display='none'">İptal</button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>

        <div class="mt-3">
            <a href="/settings/storefront" class="btn btn-outline-secondary">
                <i class="ti ti-arrow-left"></i>
                Mağaza Ayarlarına Dön
            </a>
        </div>

@section Scripts {
<script>
    function showReplyForm(reviewId) {
        var row = document.getElementById('reply-row-' + reviewId);
        row.style.display = row.style.display === 'none' ? '' : 'none';
    }
</script>
}
