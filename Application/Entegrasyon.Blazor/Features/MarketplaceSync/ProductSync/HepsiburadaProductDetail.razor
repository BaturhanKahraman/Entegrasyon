@using Entegrasyon.Entity.Dtos.Product

<MudStack Spacing="3">
    <MudPaper Class="pa-3" Elevation="1">
        <MudText Typo="Typo.subtitle1" Class="mb-2">Hepsiburada Durumu</MudText>
        <MudGrid>
            <MudItem xs="12" sm="6" md="3">
                <MudText Typo="Typo.caption" Color="Color.Dark">Durum</MudText>
                <div>
                    <MudChip T="string" Size="Size.Small"
                             Color="@ProductSyncPage.GetSyncStateColor(SyncItem.SyncState)">
                        @ProductSyncPage.GetSyncStateLabel(SyncItem.SyncState)
                    </MudChip>
                </div>
            </MudItem>
            <MudItem xs="12" sm="6" md="3">
                <MudText Typo="Typo.caption" Color="Color.Dark">Son Senkronizasyon</MudText>
                <MudText Typo="Typo.body2">@(SyncItem.LastSyncedAt?.LocalDateTime.ToString("dd.MM.yyyy HH:mm") ?? "—")</MudText>
            </MudItem>
        </MudGrid>
        @if (!string.IsNullOrEmpty(SyncItem.StatusMessage))
        {
            <MudAlert Severity="Severity.Error" Dense="true" Class="mt-2">@SyncItem.StatusMessage</MudAlert>
        }
    </MudPaper>

    <MudAlert Severity="Severity.Info">İçerik yönetimi yakında gelecek...</MudAlert>

    <AuthorizeView Policy="@AppPermissions.Marketplace.Edit">
        <MudButton Variant="Variant.Outlined" Color="Color.Primary"
                   StartIcon="@Icons.Material.Filled.Sync"
                   OnClick="HandleSync"
                   Disabled="@_isBusy">
            Sync
        </MudButton>
    </AuthorizeView>
</MudStack>
